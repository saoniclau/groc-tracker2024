#ifndef MenuOptions_H
#define MenuOptions_H

#include <iostream>
#include <fstream>
#include <string>

using namespace std;


int namefrequency(string name, string filename);
void itemStock(string filename);